<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.filipegenu.neocatapp.Controllers">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".CadastroUsuarioActivity" />
        <activity android:name=".AlteracaoUsuarioActivity" />
        <activity android:name=".MenuParoquiaActivity" />
        <activity android:name=".AlterarParoquiaActivity" />
        <activity android:name=".CadastroParoquiaActivity" />
        <activity android:name=".CadastroEventoActivity" />
        <activity android:name=".AlterarEventoActivity" />
        <activity android:name=".ConfiguracoesActivity" />
        <activity android:name=".BuscarParoquiaActivity" />
        <activity android:name=".VisualizarParoquiaActivity" />
        <activity android:name=".MenuCantosActivity" />
        <activity android:name=".BuscarCantoActivity" />
        <activity android:name=".VisualizarCantoActivity" />
        <activity android:name=".MenuRelatorioActivity" />
        <activity android:name=".CantosFavoritosActivity" />
        <activity android:name=".MudarIdiomaActivity"></activity>
    </application>

</manifest>